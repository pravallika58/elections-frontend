import { StyleSheet } from "react-native";
import { moderateScale } from "../../../constants/responsiveSizes";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  vacantSpace: {
    height: moderateScale(100),
  },
});

export default styles;
